переделать все enum в стринги и замапить

enum: names in tables: language level, skill, group, company, language,

передача данных с формы(address etc.), одна форма или несколько
передача данных в форму(запрос)
//сделать чтобы хидер оставался всё время вверху
запрос в базу на поиск по email
id повторяющихся полей(company, address, etc)
добавить объект(skill, education, etc.)|


убрать пароль из профиля
разбить мой аккаунт на много страниц
in service layer add role_id in update and save